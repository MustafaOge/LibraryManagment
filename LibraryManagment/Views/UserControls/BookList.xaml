<UserControl x:Class="LibraryManagment.Views.UserControls.BookList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibraryManagment.Views.UserControls"
             mc:Ignorable="d" 
                   d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions >
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0">
            <DataGrid BorderBrush="{x:Null}" BorderThickness="0">

            </DataGrid>
        </Border>

        <Border Background="White" Grid.Row="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btn_addBook" Style="{DynamicResource userControllerAddBook}" Content="Kitap Ekle"  Click="btn_addBook_Click"/>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>
